#include "mapping.h"
#include <unordered_map>

void map_board(int board[9][9]){

  for(int i = 0; i < 9; i++){
    for(int j = 0; j < 9; j++){
    // int row = ((i + 1) / 3) * 3 - 1; // set start row of grid
    // int col = ((i + 1) % 3) * 3 - 1; // set start col of grid
    // for (int j = 0; j < 3; j++){ //iterate over cols of grid
    //   for (int k = 0; k < 3; k++){ //iterate over rows of grid
    //     unordered_map[]
    //   }
    // }
    }
  }
}
